
# $Id$

#
#  Makefile : common section for all modules
#

vpath %.tab.h $(OBJECTDIR)
vpath %.h  $(SOURCEDIR)
vpath %.c  $(SOURCEDIR)
vpath %.cc $(SOURCEDIR)
vpath %.l  $(SOURCEDIR)
vpath %.y  $(SOURCEDIR)

