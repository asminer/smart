
// $Id$

#ifndef CTL_MSR_H
#define CTL_MSR_H

#include "../include/list.h"

class symbol_table;
class exprman;
class msr_func;

/** 
    Initialize CTL measure functions.
      @param  st  Symbol table, for help topic(s), or any "global" functions.
      @param  em  Expression manager.
      @param  L   List of measures we should add to.
*/
// void InitCTLMeasureFuncs(symbol_table* st, exprman* em, List <msr_func> *L);

#endif
